1、为了使用新特性，使用4.5.1框架。sqlite官方4.5版本只支持到4.5.1.
2、sqlite使用nuget下载来的。
3、监视目录功能：更新数据库内的有效文件，新建表存储目录，前台输入这些目录，后台定时处理这些目录。
4、跳转到文件功能
5、同步文件：数据库中有，但是已经不纳入观察的文件，要先删除，然后在插入或更新文件索引

todo：
如果不存在数据库，新建它
动态加载parser

思路：
获取指定目录下的所有文件名、路径、修改时间、大小。
（序号，路径+文件名，md5，内容，索引时间，......）表

datagridview设置：
外框样式、行宽自适应、整行选择、不显示行头列头、readonly
